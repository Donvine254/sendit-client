export type AddressFormData = {
  fullName: string;
  phone: string;
  email: string;
  region: string;
  district: string;
  address: string;
};

export type ParcelFormData = {
  description: string;
  weight: number;
};
